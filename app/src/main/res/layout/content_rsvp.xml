<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.ankita.momentwedding.RSVPActivity"
    android:background="@color/colorSelect"
    tools:showIn="@layout/activity_rsvp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="30dp"
        android:background="@drawable/img">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:background="@color/colorTransparentWrite">

            <TextView
                style="@style/Rsvp_TextView"
                android:padding="20dp"
                android:text="@string/txt_coming"/>

            <android.support.design.widget.TabLayout
                android:id="@+id/tabRsvp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:tabIndicatorColor="@color/colorWrite"
                app:tabMode="fixed"
                app:tabTextColor="@color/colorSelect"
                app:tabTextAppearance="@style/TextAppearance.AppCompat"
                app:tabSelectedTextColor="@color/colorWrite"
                app:tabBackground="@drawable/background_tab"/>

            <android.support.v4.view.ViewPager
                android:id="@+id/vpRsvp"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />

        </LinearLayout>

    </LinearLayout>

</LinearLayout>
